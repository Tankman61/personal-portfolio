export default function SplashScreen() {
    return (
        <div className="h-screen w-screen bg-black text-green-400 flex items-center justify-center text-center">
            <h1 className="text-xl md:text-2xl font-mono">
                LOADING PORTFOLIO<br />(MAX 3 SECONDS)
            </h1>
        </div>
    );
}